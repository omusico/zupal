<?
$this->placeholder('page_title')->set('Export Menu INI');

$options = array('rows' => 20, 'cols' => 80, 'readonly' => 'readonly');
echo $this->formTextarea('data', $this->data, $options);
?>
<p>Insert this block (or the good bits of it) into your modules(') ini files</p>