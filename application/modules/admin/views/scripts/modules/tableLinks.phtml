<?
	$tables = $this->adapter->fetchCol('show tables');
?>
<dl class="table_links">
<? foreach($tables as $table): ?>
	<dt><?=  new Zupal_Control_Link($table, array(
		'target' => 'table_def_' . $this->db_name,
		'module' => 'admin',
		'controller' => 'modules',
		'action' => 'tableclass',
		'database' => $this->db_name,
		'table' => $table)) ?></dt>
<? endforeach; ?>
</dl>