[fields]

elements.group_id.type = "hidden"

elements.game_id.type = "hidden"

elements.size.type = "text"

elements.mode.type = "radio"
elements.mode.options.multiOptions.absolute = "Change To"
elements.mode.options.multiOptions.relative = "Change By"
elements.mode.options.value = "absolute"

controls.submit.type = "submit"
controls.submit.options.label = "Change Group Size"

action = "/admin/ultimatum/groupresize"